{
  "name": "photosynthrudel",
  "private": true,
  "version": "0.1.0",
  "description": "A local reactive soundscape shaped by your environment, computer workload, and human-AI activity.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.js",
    "dev:ui": "vite --host localhost",
    "telemetry": "node server/server.js",
    "agent:codex": "node scripts/codex-bridge.js",
    "agent:check": "node scripts/check-codex-hooks.js",
    "agent:setup": "node scripts/setup-codex-hooks.js",
    "build": "vite build",
    "preview": "vite preview --host localhost",
    "test": "node --test"
  },
  "devDependencies": {
    "vite": "^8.3.0"
  },
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "dependencies": {
    "@strudel/soundfonts": "1.3.0",
    "@strudel/web": "1.3.0"
  }
}
